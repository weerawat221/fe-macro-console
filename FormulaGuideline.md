# 📘 Marcruro Formula Guideline (คู่มือและมาตรฐานการเขียนสูตรคำนวณ)

เอกสารนี้จัดทำขึ้นเพื่อให้ **ผู้ใช้งาน (Users)** และ **ปัญญาประดิษฐ์ (AI / LLMs)** ทำความเข้าใจไวยากรณ์ (Syntax), กฎเกณฑ์ (Rules), ฟังก์ชันที่รองรับ, และรูปแบบการเขียนสูตรคำนวณอัตโนมัติ (Derived Variables) ในโปรแกรม **Marcruro** ได้อย่างถูกต้อง 100%

---

## 📌 1. โครงสร้างและกฎเหล็กของสูตร (Core Golden Rules)

ทุกสูตรใน Marcruro ต้องปฏิบัติตามกฎ 5 ข้อต่อไปนี้อย่างเคร่งครัด:

1. **ต้องครอบด้วยปีกกา `{` และ `}` เสมอ**
   - รูปแบบ: `{ ... }`
2. **ทุกบรรทัดต้องเป็นคำสั่งกำหนดค่า (Explicit Assignment) เท่านั้น**
   - รูปแบบ: `<ตัวแปร> = <นิพจน์>`
   - ไม่อนุญาตให้เขียนนิพจน์ลอย ๆ เช่น `lan_ip.split(".")` โดยไม่มีตัวแปรรับค่า
3. **บรรทัดสุดท้ายของสูตร ต้องกำหนดค่าให้กับตัวแปรเป้าหมาย (Destination Variable) เสมอ**
   - หากกำลังเขียนสูตรให้ตัวแปร `lan_ip_1` บรรทัดสุดท้ายต้องขึ้นต้นด้วย `lan_ip_1 = ...`
4. **ตัวแปรภายใน (Local Variables / Array) มี Scope เฉพาะในสูตรนั้น ๆ**
   - การประกาศ Array ชั่วคราว เช่น `array[] = lan_ip.split(".")` จะใช้งานได้เฉพาะในสูตรนี้เท่านั้น และไม่กระทบกับตัวแปรในสูตรอื่น
   - **ข้อห้าม:** ห้ามตั้งชื่อตัวแปร Local ซ้ำกับชื่อ Variable อื่นที่มีอยู่แล้วในระบบ
5. **การแปลงชนิดข้อมูลและการคำนวณทางคณิตศาสตร์**
   - เครื่องหมาย `+` กับ String จะเป็นการต่อข้อความ (Concatenation) เช่น `"250" + 1` ➔ `"2501"`
   - เครื่องหมาย `-`, `*`, `/`, `mod` ใช้ได้กับ **ตัวเลข (Number)** เท่านั้น หากต้องการนำค่า String มาคำนวณ ต้องแปลงด้วย `toint(...)` ก่อนเสมอ

---

## 🛠️ 2. ฟังก์ชันและตัวดำเนินการที่รองรับ (Supported Functions & Operators)

### 2.1 ฟังก์ชันพื้นฐาน
| ฟังก์ชัน / Method | คำอธิบาย | ตัวอย่างการใช้งาน | ผลลัพธ์ |
|---|---|---|---|
| `str.split(separator)` | แยกข้อความเป็น Array ของ String | `arr[] = "192.168.1.1".split(".")` | `arr[0]="192", arr[1]="168", ...` |
| `str.split(separator, index)` | แยกข้อความและดึงค่า index นั้นทันที | `olt = "1/1/1:5".split(":", 0)` | `"1/1/1"` |
| `toint(value)` | แปลง String หรือ Number ให้เป็นจำนวนเต็ม | `num = toint("10") + 5` | `15` |
| `tostring(value)` | แปลง Number หรือ Object ให้เป็น String | `txt = tostring(100)` | `"100"` |

### 2.2 ตัวดำเนินการทางคณิตศาสตร์และข้อความ
| Operator | การทำงานกับตัวเลข | การทำงานกับข้อความ | ข้อผิดพลาดที่อาจเกิดขึ้น |
|---|---|---|---|
| `+` | บวกเลข (`1 + 2` ➔ `3`) | ต่อข้อความ (`"A" + "B"` ➔ `"AB"`) | ไม่มี (หากฝั่งใดเป็น String จะกลายเป็นการต่อข้อความ) |
| `-` | ลบเลข (`10 - 3` ➔ `7`) | ❌ ไม่รองรับ | Error: Operand ต้องเป็นตัวเลขเท่านั้น |
| `*` | คูณเลข (`4 * 5` ➔ `20`) | ❌ ไม่รองรับ | Error: Operand ต้องเป็นตัวเลขเท่านั้น |
| `/` | หารเลข (`10 / 2` ➔ `5`) | ❌ ไม่รองรับ | Error: ห้ามหารด้วย 0 หรือใช้กับ String |
| `mod` | หารเอาเศษ (`10 mod 3` ➔ `1`) | ❌ ไม่รองรับ | Error: Operand ต้องเป็นตัวเลขเท่านั้น |

---

## 💡 3. ตัวอย่างสูตรที่ใช้บ่อยในงานจริง (Real-World Examples)

### ตัวอย่างที่ 1: การคำนวณบวกเลข IP Octet ท้าย (+1, +2)
**โจทย์:** มีตัวแปรต้นทาง `lan_ip` (เช่น `172.17.218.17`) ต้องการคำนวณหา `lan_ip_1` (`172.17.218.18`) และ `lan_ip_2` (`172.17.218.19`)

**สูตรสำหรับ `lan_ip_1`:**
```text
{
  array[] = lan_ip.split(".")
  array[3] = toint(array[3]) + 1
  lan_ip_1 = array[0] + "." + array[1] + "." + array[2] + "." + array[3]
}
```

**สูตรสำหรับ `lan_ip_2`:**
```text
{
  array[] = lan_ip.split(".")
  array[3] = toint(array[3]) + 2
  lan_ip_2 = array[0] + "." + array[1] + "." + array[2] + "." + array[3]
}
```

---

### ตัวอย่างที่ 2: การแยกพอร์ต (Port Split) เป็น OLT และ ONU Index
**โจทย์:** มีตัวแปร `port` ค่า `1/1/1:5` ต้องการแยกเป็น `olt` (`1/1/1`) และ `onu_idx` (`5`)

**สูตรสำหรับ `olt` (ดึงส่วนหน้าตัวคั่น `:`):**
```text
{
  olt = port.split(":", 0)
}
```

**สูตรสำหรับ `onu_idx` (ดึงส่วนหลังตัวคั่น `:`):**
```text
{
  onu_idx = port.split(":", 1)
}
```

---

### ตัวอย่างที่ 3: การคำนวณหา Network IP หรือ Subnet Broadcast (+3, +4)
**โจทย์:** ต้องการหา IP ตัวที่ 4 ของวง LAN ให้ตัวแปร `lan_ip_host4`
```text
{
  octets[] = lan_ip.split(".")
  octets[3] = toint(octets[3]) + 4
  lan_ip_host4 = octets[0] + "." + octets[1] + "." + octets[2] + "." + octets[3]
}
```

---

### ตัวอย่างที่ 4: การต่อข้อความ (String Concatenation) สำหรับสร้าง Profile Name
**โจทย์:** รวมรหัส `sr_ap` กับ `vlan` เพื่อตั้งชื่อ Profile เช่น `SR701234_VLAN214`
```text
{
  profile_name = sr_ap + "_VLAN" + tostring(vlan)
}
```

---

## 🤖 4. AI Prompt Template (คำสั่งสำหรับส่งให้ AI ตัวอื่นเขียนสูตร)

หากต้องการให้ AI (เช่น ChatGPT, Claude, DeepSeek หรือ Gemini) ช่วยเขียนสูตรสำหรับระบบ Marcruro สามารถคัดลอก Prompt ด้านล่างนี้ไปสั่ง AI ได้ทันที:

```markdown
คุณคือผู้เชี่ยวชาญการเขียนสูตรสำหรับระบบ Marcruro Formula Engine
กรุณาเขียนสูตรตามกฎไวยากรณ์ (Grammar) ต่อไปนี้อย่างเคร่งครัด:

1. ครอบสูตรด้วย `{` และ `}` เสมอ
2. ทุกบรรทัดต้องเป็นคำสั่งกำหนดค่า `<variable> = <expression>`
3. บรรทัดสุดท้ายต้องกำหนดค่าให้กับตัวแปรปลายทาง (<target_variable>) เสมอ
4. ฟังก์ชันที่รองรับ:
   - `str.split(separator)` หรือ `str.split(separator, index)`
   - `toint(value)` สำหรับแปลงเป็นตัวเลข
   - `tostring(value)` สำหรับแปลงเป็นข้อความ
   - ตัวดำเนินการ: `+` (บวกเลขหรือต่อข้อความ), `-`, `*`, `/`, `mod` (เฉพาะตัวเลข)
5. ตัวแปร Array ชั่วคราวประกาศด้วย `array[] = str.split("...")` และอ้างอิงด้วย `array[0]`

โจทย์ที่ต้องการ:
- ตัวแปรต้นทาง: [ระบุชื่อตัวแปร เช่น lan_ip, port, sr_ap]
- ตัวแปรเป้าหมายที่ต้องการสร้าง: [ระบุชื่อตัวแปร เช่น lan_ip_1, olt]
- รูปแบบที่ต้องการ: [อธิบายสิ่งที่ต้องการคำนวณหรือแยกค่า]

โปรดส่งออกเฉพาะโค้ดสูตรในบล็อก { ... }
```

---

## ⚠️ 5. ข้อผิดพลาดที่พบบ่อยและวิธีป้องกัน (Troubleshooting & Errors)

| อาการ / ข้อความ Error | สาเหตุ | วิธีแก้ไข |
|---|---|---|
| `Formula error: บรรทัดสุดท้ายของสูตรต้องกำหนดค่าให้กับตัวแปร '...'` | ลืมกำหนดค่าให้ตัวแปรปลายทางที่บรรทัดสุดท้าย | ตรวจสอบบรรทัดสุดท้ายให้เป็น `<target_variable> = ...` |
| `Formula error: ตัวดำเนินการ '-' ใช้ได้กับตัวเลขเท่านั้น` | นำตัวแปร String มาลบกันโดยตรง | ใช้ `toint(...)` ครอบตัวแปรก่อนนำมาลบหรือคูณหาร |
| `Formula error: ไม่พบตัวคั่น ':' ในค่าของ port` | ค่าต้นทางไม่มีเครื่องหมายหรือตัวคั่นที่ระบุใน `.split()` | ตรวจสอบข้อมูล Input ต้นทาง หรือใช้ตัวคั่นที่ถูกต้อง |
| `Formula error: index เกินขอบเขต` | ดึง Array element เกินขนาดผลลัพธ์ | ตรวจสอบจำนวนชิ้นที่แยกได้จาก `.split()` (เริ่มนับที่ 0) |
| `Circular dependency detected` | สูตรตัวแปรอ้างอิงวนลูปหากัน (A ➔ B ➔ A) | ปรับโครงสร้างตัวแปรไม่ให้อ้างอิงย้อนกลับ |
| `Validation error: octet ที่ ... มีค่า ... เกินช่วง (0-255)` | บวกค่า Octet ของ IP แล้วเกิน 255 | ตรวจสอบการคำนวณ Octet ให้ถูกต้องตามมาตรฐาน IPv4 |
